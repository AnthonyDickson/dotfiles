{
  stylix = {
    targets = {
      # Assume you are using "screenshot" as the background for hyprlock.
      hyprlock.useWallpaper = false;
      # Assume you are customizing the css styles for waybar yourself.
      waybar.enable = false;
      # Stylix theme for helix doesn't seem to work correctly.
      helix.enable = false;
    };
  };
}
