{
  stylix = {
    targets = {
      # Assume you are using "screenshot" as the background.
      hyprlock.useWallpaper = false;
      # Assume you are customizing the css styles yourself.
      waybar.enable = false;
    };
  };
}
